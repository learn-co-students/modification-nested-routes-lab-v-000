<%= form_for @song  do |f| %>
  <%= f.label :title %>

  <!-- new code start -->
  <%= song_artist_id(@song) %>
  <!-- new code finish -->

  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %>
  <br />

  <%= f.submit %>
<% end %>
