<%= form_for @song  do |f| %>
  <%= artist_select(@song.artist, @song) %>

  <%= f.label :title %>
  <%= f.text_field :title %><br>

  <br><br>
  <% if @song.artist.nil? %>
    <%= f.label :artist_name %>
    <%= f.text_field :artist_name %><br>
  <% end %>

  <br><br>

  <%= f.submit %>
<% end %>
