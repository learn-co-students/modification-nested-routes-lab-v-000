<%= form_for @song do |f| %>
  <h4><%= artist_select(@song.artist, @song) %></h4>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %><br>

  <%= f.submit %>
<% end %>
