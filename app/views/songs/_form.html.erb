<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= select_tag "song[artist_id]", options_from_collection_for_select(Artist.all, :id, :name) %>
  <%= artist_select(@song.artist_id, @song) %>


  <%= f.submit %>
<% end %>
