<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>
    <%=display_artist(@song)%>
    <%=artist_select(@song.artist, @song)%>
  <%= f.hidden_field :artist_id %>
  <br />

  <%= f.submit %>
<% end %>
