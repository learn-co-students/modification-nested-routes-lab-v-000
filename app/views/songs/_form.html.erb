<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>
  <% binding.pry %>
  <%= artist_id_field(@song) %>
  <br />

  <%= f.submit %>
<% end %>
