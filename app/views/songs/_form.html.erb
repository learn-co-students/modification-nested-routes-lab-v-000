<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %><br>
  <!-- has a selector for artist if no artist_id -->
  <%= artist_select(@song, @song.artist) %>
  <%= f.submit %>
<% end %>
